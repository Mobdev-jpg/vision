export default function handler(req,res){const proto=(req.headers['x-forwarded-proto']||'https').split(',')[0].trim();const host=(req.headers['x-forwarded-host']||req.headers.host||'').split(',')[0].trim();const base=(process.env.PUBLIC_SITE_URL||`${proto}://${host}`).replace(/\/$/,'');res.setHeader('Content-Type','text/plain; charset=utf-8');res.status(200).send(`User-agent: *\nAllow: /\nDisallow: /dashboard.html\nDisallow: /login.html\nSitemap: ${base}/sitemap.xml\n`)}
