import {createClient} from '@supabase/supabase-js';
export default async function handler(req,res){try{const base=process.env.PUBLIC_SITE_URL||'https://your-domain.com';const db=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const {data}=await db.from('businesses').select('slug,updated_at').eq('published',true).limit(50000);const urls=[`${base}/`,`${base}/explore.html`,`${base}/pricing.html`,...(data||[]).map(b=>`${base}/business/${encodeURIComponent(b.slug)}`)];const xml='<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+urls.map(u=>`<url><loc>${u}</loc></url>`).join('')+'</urlset>';res.setHeader('Content-Type','application/xml');res.setHeader('Cache-Control','s-maxage=600, stale-while-revalidate=3600');res.status(200).send(xml);}catch(e){res.status(500).send('');}}
