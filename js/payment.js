let payCfg,supaPay;
async function setup(){payCfg=await fetch('/api/config').then(r=>r.json());supaPay=supabase.createClient(payCfg.supabaseUrl,payCfg.supabaseAnonKey);pay.onclick=start}
async function start(){pay.disabled=true;payMsg.textContent='Checking account…';const {data:{session}}=await supaPay.auth.getSession();if(!session){location.href='/login.html';return}const {data:biz}=await supaPay.from('businesses').select('id,name').eq('owner_id',session.user.id).maybeSingle();const r=await fetch('/api/create-subscription',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({business_id:biz?.id||null})});const j=await r.json();if(!r.ok){payMsg.textContent=j.error||'Payment setup failed';pay.disabled=false;return}const options={key:j.keyId,subscription_id:j.subscriptionId,name:'TripNest',description:'TripNest Business — ₹699/month',prefill:{contact:session.user.phone,email:session.user.email},theme:{color:'#0b6b4c'},handler:async response=>{const c=await fetch('/api/confirm-subscription',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({...response,business_id:biz?.id||null})});payMsg.textContent=c.ok?'Payment authorised. Your subscription is being activated.':'Payment received; waiting for subscription confirmation.'}};new Razorpay(options).open();pay.disabled=false}
setup();
