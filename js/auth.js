let sb,signup=false;
async function init(){const c=await fetch('/api/config').then(r=>r.json());sb=supabase.createClient(c.supabaseUrl,c.supabaseAnonKey);const {data:{session}}=await sb.auth.getSession();if(session)location.href='/dashboard.html';google.onclick=async()=>{const {error}=await sb.auth.signInWithOAuth({provider:'google',options:{redirectTo:location.origin+'/dashboard.html'}});if(error)msg.textContent=error.message};loginMode.onclick=()=>setMode(false);signupMode.onclick=()=>setMode(true);authForm.onsubmit=submit}
function setMode(v){signup=v;formTitle.textContent=v?'Create account':'Login';submitBtn.textContent=v?'Create account':'Login';loginMode.classList.toggle('active',!v);signupMode.classList.toggle('active',v);msg.textContent=''}
async function submit(e){e.preventDefault();msg.textContent='Working…';const p=phone.value.trim(),pw=password.value;if(!p||pw.length<6)return msg.textContent='Enter a valid mobile number and a password with 6+ characters.';let r;if(signup)r=await sb.auth.signUp({phone:p,password:pw,options:{data:{full_name:'TripNest traveler'}}});else r=await sb.auth.signInWithPassword({phone:p,password:pw});if(r.error)return msg.textContent=r.error.message;msg.textContent=signup?'Account created. You can now open the dashboard.':'Logged in ✓';setTimeout(()=>location.href='/dashboard.html',500)}
init();
